import { memo } from "react";
import HeroSection from "./heroSection"

const ProjectManagerDashboard = () => {
    return (
        <main>
            <HeroSection />
        </main>
    )
}

export default memo(ProjectManagerDashboard);